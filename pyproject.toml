[project]
name = "actionos-cli"
version = "0.1.0"
description = "Personal AI assistant on Telegram, powered by official AI CLIs"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tomli>=2.0; python_version < '3.11'",
]

[project.scripts]
actionos = "src.main:main"
